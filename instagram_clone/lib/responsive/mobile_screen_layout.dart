import 'package:flutter/material.dart';

class MobilScreenLayout extends StatelessWidget {
    MobilScreenLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return   Scaffold(
      body: Center(
        child: Text("this is mobile"),
      ),
    );
  }
}
